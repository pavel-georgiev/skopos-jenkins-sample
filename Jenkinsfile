stage('Build') {
    node {
        checkout scm
        echo "Building..."
        #def app = docker.build "your-project-name"
    }
}

#
stage('Test') {
    node {
        echo "Testing..."
    }
}


stage('Deploy') {
    node {
        echo "Deploying..."
    }
}
